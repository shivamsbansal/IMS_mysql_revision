<%=  if @category != 'All'
		@items = Category.find(@category).items
		select_tag(:item_id, options_from_collection_for_select(@items, "id","nameItem"))
		else
		@items = Item.all
		select_tag(:item_id,content_tag(:option,'All',:value=>"All") + options_from_collection_for_select(@items, "id","nameItem"))
		end
%>
